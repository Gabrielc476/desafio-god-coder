/**
 * DTO de entrada (Input) para o FindAllChannelsUseCase.
 * (Não requer parâmetros).
 */
export interface IFindAllChannelsInput {
  // Esta interface pode ficar vazia
  // ou pode receber parâmetros de paginação no futuro.
}